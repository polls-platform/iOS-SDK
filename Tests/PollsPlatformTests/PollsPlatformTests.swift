    import XCTest
    @testable import PollsPlatform

    // jazzy --swift-build-tool xcodebuild --build-tool-arguments -scheme,PollsPlatform,-destination,generic/platform=iOS --readme SDK-REFERENCE-README.md
    
    final class PollsPlatformTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
//            XCTAssertEqual(PollsPlatform().text, "Hello, World!")
        }
    }
